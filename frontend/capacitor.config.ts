import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.smarttemp.monitor",
  appName: "Smart Temperature Monitoring",
  webDir: "dist",
};

export default config;